import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'mood-tracker',
  webDir: 'dist/mood-tracker/browser'
};

export default config;
